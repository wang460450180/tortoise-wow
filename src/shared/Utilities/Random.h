#ifndef MANGOS_RANDOM_COMPATIBILITY_H
#define MANGOS_RANDOM_COMPATIBILITY_H

// Eluna's VMaNGOS adapter uses the newer Utilities/Random.h include path.
// Turtle WoW keeps the same random helpers in the legacy shared/Util.h file.
#include "Util.h"

#endif
